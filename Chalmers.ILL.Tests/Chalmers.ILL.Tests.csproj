<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Chalmers.ILL.Tests</RootNamespace>
    <AssemblyName>Chalmers.ILL.Tests</AssemblyName>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <IsPackable>false</IsPackable>
    <Nullable>disable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="log4net" Version="2.0.12" />
    <PackageReference Include="System.Configuration.ConfigurationManager" Version="9.0.0" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="MSTest.TestAdapter" Version="3.6.4" />
    <PackageReference Include="MSTest.TestFramework" Version="3.6.4" />
    <!-- Browser-driven verification (brytpunkten efter fas 2). Points at the devcontainer's
         apt-installed system Chromium via CHROMIUM_EXECUTABLE_PATH rather than downloading its
         own browser build - that download comes from a CDN outside the runtime firewall's
         allowlist. -->
    <PackageReference Include="Microsoft.Playwright" Version="1.49.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Chalmers.ILL\Chalmers.ILL.csproj" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="Mail\Data\reprints-desk-mail.txt" />
    <EmbeddedResource Include="Mail\Data\reprints-desk-mail-links-removed.txt" />
  </ItemGroup>

</Project>
