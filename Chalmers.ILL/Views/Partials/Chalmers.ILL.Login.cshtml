@model Chalmers.ILL.Models.LoginModel


@if (Request.Query["error"] == "invalid-member")
{
    <div class="alert alert-danger"><strong>Felaktig inloggning.</strong> Ditt login eller lösenord stämmer inte.</div>
}

@if (Request.Query["error"] == "invalid-model")
{
    <div class="alert alert-danger"><strong>Värden saknas.</strong> Du måste fylla i fälten.</div>
}
@using (Html.BeginForm("HandleLogin", "LoginSurface", FormMethod.Post, new { @role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <div class="form-group">
        @Html.LabelFor(model => model.Login)
        @Html.TextBoxFor(model => model.Login, new { @class = "form-control" })
        @Html.ValidationMessageFor(model => model.Login)
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.Password)
        @Html.PasswordFor(model => model.Password, new { @class = "form-control" })
        @Html.ValidationMessageFor(model => model.Password)
    </div>
    <button type="submit" class="btn btn-primary">Logga in</button>
    <script type="text/javascript">
        $(document).ready(function () { $("#Login").focus(); });
    </script>
}
